#Tuples are ordered collection of data items. They stores multiple items in a single variable. Tuple items are seperated by commas and enclosed within round brackets(). Tuples are unchangedable meaning we can not alter them after creation. 
tup = (1, 2, 3, 342, 43, "Arnab", True)
print(type(tup), tup)
print(tup[0])
print(tup[-1])
print(tup[2])